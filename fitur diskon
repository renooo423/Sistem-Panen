def hitung_total_panen(jumlah, harga):
    return jumlah * harga

def hitung_diskon(total_harga, diskon):
    return total_harga - (total_harga * (diskon / 100))

total = hitung_total_panen(100, 5000)
print(f"Total panen: Rp{total}")
print(f"Total setelah diskon 10%: Rp{hitung_diskon(total, 10)}")
